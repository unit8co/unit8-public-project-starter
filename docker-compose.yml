services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      APP_ENVIRONMENT: docker
    ports:
      - "8000:8000"
    command: ["uv", "run", "agentic-starter", "serve", "--host", "0.0.0.0", "--port", "8000"]
